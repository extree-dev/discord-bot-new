// Отключено по решению администратора — бот вообще не должен сам писать
// участникам в личные сообщения (уведомление о наказании, апелляция
// прямо из DM и всё остальное, что раньше здесь жило). Единственный
// канал личных сообщений от бота теперь — /dm, отправляет вручную
// администратор. Функция и сигнатура оставлены как есть — вызывающие
// места (ban.js, timeout.js, tickets/model.js punishReportedUser) просто
// ничего не делают, чтобы не выдирать вызовы по всему коду ради
// выключения одной фичи, и чтобы можно было включить обратно одной
// правкой, если понадобится.
async function sendPunishmentDm() {}

module.exports = { sendPunishmentDm };
